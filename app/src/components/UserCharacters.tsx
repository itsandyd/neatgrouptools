import React from "react";

const UserCharacters: React.FC = () => {
  // Fetch and display user characters here
  return <div>Your characters will be displayed here</div>;
};

export default UserCharacters;
